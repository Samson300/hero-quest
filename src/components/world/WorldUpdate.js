import store from '../../config/store';
import { SPRITE_SIZE, MAP_WIDTH, MAP_HEIGHT } from '../../config/constants';

export default function WorldUpdate(world) {
    function getNewMap() {
        
    }
}